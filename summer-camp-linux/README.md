# Summer Camp Linux Practice

这是一个用于 Linux / Shell 基础练习的模拟 AI 工程项目。

## 目录说明

- `data/`：示例数据
- `configs/`：示例配置
- `src/`：示例程序
- `scripts/`：Shell 脚本
- `logs/`：日志

## 运行示例

```bash
python3 src/train.py
```

你不需要理解 `src/` 中 Python 程序的具体实现。

本项目只使用 Python 标准库，不依赖 NumPy、Pandas、PyTorch 或 GPU。
